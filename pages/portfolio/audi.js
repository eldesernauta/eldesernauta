import Nav from "../../components/Nav/nav";
import Layout from '../../components/Layout/layout';

const Audi = () => {
    return (
        <Layout>
            <Nav />
            <div className="section w-full h-screen bg-dark">

            </div>
        </Layout>
    );
}

export default Audi;